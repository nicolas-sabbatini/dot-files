#!/bin/bash
set -e

sudo -p "You must be sudo: " printf "" || exit 1

if command dnf &>/dev/null; then
  echo "============================ 💻 Updating  System 💻 ============================"
  sudo dnf upgrade -y
  echo "================================================================================"
fi

if command apt --version &>/dev/null; then
  echo "============================ 💻 Updating  System 💻 ============================"
  sudo apt update && sudo apt upgrade -y
  echo "================================================================================"
fi

if command snap &>/dev/null; then
  echo "============================ 💻 Updating  Snaps 💻 ============================="
  sudo snap refresh
  echo "================================================================================"
fi

if command gup &>/dev/null; then
  echo "=============================== 🦫 Update  GO 🦫 ==============================="
  gup update
  echo "================================================================================"
fi

if command flatpak --version &>/dev/null; then
  echo "=============================  Cleaning Flatpak  ============================="
  flatpak uninstall --unused -y
  echo "================================================================================"
  echo "=============================  Updating Flatpak  ============================="
  flatpak update -y
  echo "================================================================================"
fi

if command -v rustup &>/dev/null; then
  echo "============================= 🦀 Updating  Rust 🦀 ============================="
  rustup update
  echo "================================================================================"
  echo "============================= 📦 Updating cargo 📦 ============================="
  cargo install-update -a
  echo "================================================================================"
fi

if command -v deno &>/dev/null; then
  echo "============================= 🦕 Updating  Deno 🦕 ============================="
  deno upgrade
  echo "================================================================================"
fi

echo "👏 DONE! 👏"
